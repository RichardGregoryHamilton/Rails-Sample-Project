<% @title = "Wii U Games" %>

<% content_for :meta do %>
  <meta name="description" content="Wii U Games">
<% end %>

<h3>All Wii U Games</h3>

<%= render :partial => "pagination_console_header_one" %>
<%= will_paginate (@g) %>

<table class = "console-table">

  <%= render :partial => "console_table_head" %>
  <%= render :partial => "new_console_body" %>
  
</table>

<%= will_paginate (@g) %>

<div class = 'center'>
  <p>We found <%= pluralize(Game.where(console: 'Wii U').count, 'Wii U game') %></p>
  <p>
    <%= link_to 'Wii U Home', wii_u_path, :class => 'btn btn-info btn-lg active' %>
  </p>
</div>