<% @title = "Platform" %>

<% content_for :meta do %>
  <meta name="description" content="Platform Games">
<% end %>

<h3>Platform Games</h3>

<table class = "console-table">

  <%= render :partial => "genre_table_head" %>
  <%= render :partial => "genre_table_body" %>
  
</table>

<div class = 'center'>
  <p>We found <%= pluralize(Game.where(genre: 'Platform').count, 'Platform game') %></p>
  <p>
    <%= link_to 'Back', platform_path, :class => "btn btn-warning btn-lg active" %>
  </p>
</div>