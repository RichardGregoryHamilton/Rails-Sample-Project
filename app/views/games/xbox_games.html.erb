<% @title = "Xbox Games" %>

<% content_for :meta do %>
  <meta name="description" content="Xbox Games">
<% end %>

<h3>All Xbox Games</h3>

<%= render :partial => "pagination_console_header" %>
<%= will_paginate (@g) %>

<table class = "console-table margin-auto">

  <%= render :partial => "console_table_head" %>
  <%= render :partial => "new_table_body" %>
  
</table>

<%= will_paginate (@g) %>

<div class = 'center'>
  <p>We found <%= pluralize(Game.where(console: 'Xbox').count, 'Xbox game') %></p>
  <p>
    <%= link_to 'Xbox Home', xbox_path, :class => 'btn btn-info btn-lg active' %>
  </p>
</div>