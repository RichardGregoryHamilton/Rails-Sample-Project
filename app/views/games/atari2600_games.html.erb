<% @title = "Atari 2600 Games" %>

<% content_for :meta do %>
  <meta name="description" content="Atari 2600 Games">
<% end %>

<h3>All Atari 2600 Games</h3>

<%= render :partial => "pagination_console_header" %>
<%= will_paginate (@g) %>

<table class = "console-table">

  <%= render :partial => "console_table_head" %>
  <%= render :partial => "new_table_body" %>
  
</table>

<%= will_paginate (@g) %>

<div class = 'center'>
  <p>We found <%= pluralize(Game.where(console: 'Atari 2600').count, 'Atari 2600 game') %></p>
  <p>
    <%= link_to 'Atari Home', atari2600_path, :class => 'btn btn-info btn-lg active' %>
  </p>
</div>