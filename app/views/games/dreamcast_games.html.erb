<% @title = "Dreamcast Games" %>

<% content_for :meta do %>
  <meta name="description" content="Dreamcast Games">
<% end %>

<h3>All NES Games</h3>

<table class = "console-table">

  <%= render :partial => "console_table_head" %>
  <%= render :partial => "new_table_body" %>
  
</table>

<div class = 'center'>
  <p>We found <%= pluralize(Game.where(console: 'Dreamcast').count, 'Dreamcast game') %></p>
  <p>
    <%= link_to 'Dreamcast Home', dreamcast_path, :class => 'btn btn-info btn-lg active' %>
  </p>
</div>