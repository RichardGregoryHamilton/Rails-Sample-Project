<% @title = "Playstation Vita Games" %>

<h3>All Playstation Vita Games</h3>

<table class = "console-table">

  <%= render :partial => "console_table_head" %>
  <%= render :partial => "new_table_body" %>
  
</table>

<div class = 'center'>
  <p>We found <%= pluralize(Game.where(console: 'Vita').count, 'Vita game') %></p>
  <p>
    <%= link_to 'Vita Home', vita_path, :class => 'btn btn-info btn-lg active' %>
  </p>
</div>